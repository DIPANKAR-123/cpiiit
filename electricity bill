#include <stdio.h>

int main()
{
    float x,y,z,t;
    y=100/1.2;
    t=400/1.5;
    printf("enter the units consumed by you\n");
    scanf("%f",&x);
    
    if  (x<y){
    printf("you need to pay the min bill of rs 100");}
    else if (x>=y && x<=199){
    z=x*1.2;
    printf("you need to pay %f",z);}
    
    else if (x>199 && x<=t){
    z=x*1.5;
    printf("you need to pay %f",z);}
    
     else if (x>t && x<=400){
    z=x*1.5+200;
    printf("you need to pay %f",z);}
    
    else if (x>400 && x<=600){
    z=x*1.8+200;
    printf("you need to pay %f",z);}
    else if (x>600){
    z=x*2.0+200;
    printf("you need to pay %f",z);

    }
    return 0;
}